server:
  port: 8085  # Puedes cambiar el puerto si deseas

spring:
  application:
    name: ms-logistica

  datasource:
    url: jdbc:oracle:thin:@localhost:1526/free
    username: C##logistica
    password: logistica123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: true
