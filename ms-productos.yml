server:
  port: 0  # Para que el puerto se asigne automáticamente si quieres

spring:
  application:
    name: ms-productos

  datasource:
    url: jdbc:oracle:thin:@localhost:1523/free
    username: C##productos
    password: productos123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: true

