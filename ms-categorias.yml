server:
  port: 8084  # Se puede dejar en 0 si usas puertos dinámicos o lo defines en bootstrap.yml

spring:
  application:
    name: ms-categorias

  datasource:
    url: jdbc:oracle:thin:@localhost:1524/free
    username: C##categorias
    password: categorias123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: true

